# Never commit the login session or captured tokens/data
tabit-profile/
out/
node_modules/
*.log
agent.lock
# Holds the ingest URL + shared secret - must never be committed
sync-config.json
# Holds the Tabit user id + passcode for auto-relogin - must never be committed
agent-credentials.json
