# שדרוג פאנל הניהול - Audit, תוכנית ויישום

> מסמך עבודה חי. חלק א' - ממצאי Audit. חלק ב' - תוכנית. חלק ג' (בסוף העבודה) - מה בוצע בפועל.
> תאריך: 2026-07-22.

---

## חלק א' - מיפוי וממצאים

### 1. מיפוי המערכת הקיימת

| שכבה | מימוש |
|---|---|
| Framework | Next.js 15 (App Router), React 19, Tailwind CSS v4, TypeScript |
| פאנל | עמוד client יחיד `src/app/admin/page.tsx` עם 7 טאבים (state מקומי, בלי router) |
| Auth | `ADMIN_TOKEN` ב-env, נשלח כ-header `x-admin-token`, נשמר ב-localStorage |
| DB | ממשק `Repository` עם שני מימושים: קובץ JSON (פיתוח) / Postgres-Neon (פרודקשן) |
| רענון | Polling: שיחות כל 4 שניות, פרטי שיחה כל 4 שניות, סטטיסטיקות כל 15 שניות |
| ערוצים | WhatsApp Cloud API, Messenger, Instagram - אדפטרים ב-`src/lib/channels` |
| ידע נלמד | טבלת `learned_qa` (open/answered); התשובות מוזרקות ל-System Prompt בכל תור |
| מידע עסקי | JSON ב-settings (`business_config`), ברירת מחדל בקוד |
| מדיה | JSON ב-settings (`media_library`), קבצים ב-Vercel Blob (העלאת client ישירה) |

### 2. מה בנוי טוב (לשמר, לא לשכתב)

- הפרדת שכבות נקייה: UI → `/api/admin/*` → `admin-service` → `Repository`. אין גישה ישירה ל-DB מה-UI.
- מנגנון bot/human/closed + botPaused לכל שיחה, עם יומן פעילות (system messages) - עובד ומתועד.
- מענה נציג שולח קודם לערוץ האמיתי ורק אז נשמר - כשל שליחה מציג שגיאה אמיתית ולא "הצלחה" כוזבת.
- העלאת מדיה ישירה ל-Blob עם הגבלת סוגי קבצים וגודל בצד השרת.
- Theme tokens (CSS variables) עקביים, RTL מלא, dark mode.
- retrieval דטרמיניסטי של קטגוריות תפריט (חיסכון עלות) - לא לגעת.

### 3. בעיות שנמצאו

**P0 - חוסמות עבודה שוטפת**
1. **auto-zoom במובייל**: כל ה-inputs/textareas עם font-size של 14px ומטה (text-sm/text-xs). iOS Safari מבצע זום אוטומטי לכל שדה מתחת ל-16px. אין תיקון גלובלי.
2. **אין עריכת ידע קיים**: `learned_qa` תומך רק במענה ראשוני ומחיקה. אין PATCH ב-API, אין UI לעריכה.
3. **טיוטה גלובלית אחת בתיבת הפניות**: מעבר בין שיחות משאיר את אותה טיוטה (זליגה בין לקוחות!), והטיוטה אובדת ברענון.
4. **ביצועים בסקייל**: `listConversations` בפאנל מושך את **כל ההודעות מכל הזמנים** (`getAllMessages`) כל 4 שניות. עם אלפי הודעות זה קורס. גם `getChannelHealth` עושה זאת.
5. **חוסר-אבטחה ב-fallback**: `isAdminAuthorized` מחזיר true כש-ADMIN_TOKEN לא מוגדר - גם בפרודקשן. השוואת token לא constant-time. אותו דפוס במסלול העלאת המדיה.

**P1 - פוגעות בחוויה**
6. גלילה אוטומטית לתחתית בכל הודעה חדשה גם כשקוראים היסטוריה למעלה.
7. אין שמירת מיקום גלילה ברשימה בחזרה משיחה במובייל (display:none מאפס scroll).
8. מסננים וחיפוש אובדים במעבר טאב (ה-Inbox נעקר מה-DOM).
9. אין אינדיקציית "לא נקרא" בשום מקום.
10. אין הפרדת שיחות סגורות ("הכל" כולל סגורות), אין "פתח מחדש" גלוי.
11. `100vh` בכל מקום - נשבר עם מקלדת פתוחה ב-iOS (שדה ההקלדה מוסתר). צריך dvh.
12. עריכת מידע עסקי: מעבר טאב זורק את כל השינויים בלי אזהרה (אובדן נתונים שקט).
13. אין הפרדת ימים בין הודעות; אין dir=auto להודעות עם עברית+URL; קישורים לא לחיצים.
14. דאשבורד דקורטיבי: אף כרטיס לא לחיץ, "ממתינות לטיפול" סופר רק status=human ולא שיחות שממתינות בפועל.
15. `outline-none` בכל השדות בלי focus-visible חלופי - ניווט מקלדת שבור.
16. כפתור חזרה משיחה במובייל: "›" זעיר, בלי טקסט, מתחת ל-44px.
17. לחיצה כפולה מהירה על "שלח" עלולה לשלוח פעמיים (busy הוא state אסינכרוני, אין ref guard).
18. מחיקות בלי אישור (מדיה, ידע).
19. אין empty/loading אמין: לפני טעינה ראשונה מוצג "אין שיחות בסינון הזה" (שקר), אין באנר כשה-polling נכשל (offline).
20. TestChat בולע שגיאות אמיתיות ("(שגיאה בבדיקה)" בלבד).

**P2 - שיפורים**
21. אין חיפוש בידע שנלמד; אין תאריכים; אין "בדוק בבוט" אחרי לימוד.
22. מדיה: URL ארוך כשדה מרכזי; אין העתקת URL; אין אישור מחיקה.
23. סרגל שמירה צף במידע עסקי מכסה את ה-sidebar בדסקטופ ולא מכבד safe-area במובייל.
24. אין מניעת דריסה מקבילה בעריכת ידע (שני מכשירים).

### 4. ממצאי אבטחה (יושלמו ע"י סריקת עומק - ראו דוח סיום)

- אימות webhooks עם חתימת Meta קיים (`meta-signature.ts`).
- ה-UI לא מרנדר HTML מהודעות (React escaping) - בטוח; הוספת קישורים לחיצים תיעשה עם whitelist ל-http/https בלבד + noopener.
- שגיאות API מוחזרות עם message גולמי מהשרת - סבל נמוך, אבל שגיאות ספריות עלולות לחשוף פרטים פנימיים; נצמצם.

---

## חלק ב' - תוכנית

### ארכיטקטורת מידע (ניווט)

הסדר משתנה לפי תדירות שימוש אמיתית: **תיבת פניות** (עיקר היום-יום) → **ידע** (לימוד הבוט) → **דשבורד** → מידע עסקי → מדיה → בדיקת בוט → הגדרות.

- **דסקטופ**: sidebar קבוע (נשמר).
- **מובייל**: bottom navigation עם 4 היעדים התדירים (תיבת פניות, ידע, דשבורד, עוד) + מגירה ל"עוד". ה-bottom nav מוסתר כששיחה פתוחה כדי לפנות מקום לשדה ההקלדה.
- מסך אחד בכל רגע במובייל: רשימה ⇄ שיחה, עם כפתור חזרה גדול וברור.

### שינויי Backend

| # | שינוי | קבצים |
|---|---|---|
| B1 | `getConversationSummaries()` - שאילתת סיכום אחת (JOIN + subqueries) במקום משיכת כל ההודעות | db/types, file-repo, postgres-repo, admin-service |
| B2 | `updateLearnedQA(id, {question?, answer?})` + עמודת `updated_at` + הגנת דריסה (409 על baseTs לא תואם) | db/*, admin-service, API PATCH+POST |
| B3 | יצירת ידע ישירות (שאלה+תשובה) מהפאנל | admin-service, API POST |
| B4 | הקשחת auth: deny-by-default בפרודקשן בלי ADMIN_TOKEN, timingSafeEqual, יישור מסלול ההעלאה | admin-auth, media/upload |
| B5 | idempotency לתשובת נציג (אותה תשובה פעמיים בתוך שניות = פעולה אחת) | admin-service |
| B6 | `awaitingCount` אמיתי בסטטיסטיקות | admin-service |

הכל תוספות תואמות-לאחור: אין מחיקת API, אין מיגרציה הרסנית (רק `ADD COLUMN IF NOT EXISTS`).

### שינויי Frontend

- **גלובלי**: תיקון zoom (16px לשדות במובייל, בלי לחסום זום משתמש), focus-visible, dvh, text-size-adjust, viewport מפורש.
- **Shell**: מבנה h-dvh עם גלילה פנימית, bottom nav, באנר offline, מצב "טוען" אמיתי.
- **תיבת פניות**: טיוטות פר-שיחה (localStorage), שמירת מיקום רשימה, מסננים ב-sessionStorage, unread dot, גלילה חכמה, הפרדת ימים, מסנן "סגורות" + פתיחה מחדש, מניעת שליחה כפולה (ref), retry על כשל, קישורים בטוחים, dir=auto.
- **ידע**: חיפוש, עריכה מלאה (שאלה+תשובה) עם שמירה/ביטול, תאריכים, אישור מחיקה, הוספת ידע יזומה, "בדוק בבוט" שקופץ ל-TestChat עם השאלה.
- **דשבורד**: כרטיסים לחיצים → תיבת פניות מסוננת / ידע; מונה "ממתינות למענה" אמיתי.
- **מידע עסקי**: אזהרת שינויים לא שמורים (מעבר טאב + beforeunload), תיקון סרגל שמירה.
- **מדיה**: העתקת URL, אישור מחיקה, שדה URL מוצנע.
- **בדיקת בוט**: שגיאות אמיתיות, prefill משאלה שנלמדה.

### סיכונים ו-rollback

- כל השינויים ב-branch העבודה; `git revert`/checkout מחזיר את המצב הקודם. אין שינוי סכימה הרסני (רק ADD COLUMN). Deploy קודם נשאר ב-Vercel וניתן ל-promote חזרה.
- שינוי ה-auth עלול לנעול אם ADMIN_TOKEN לא מוגדר בפרודקשן - לפי תיעוד ה-ops הוא מוגדר; ההתנהגות בפיתוח נשארת פתוחה.

### Definition of Done

build + typecheck + lint נקיים; סקריפט בדיקות שירות עובר; כל התרחישים ב-QA-CHECKLIST הרלוונטיים לפאנל נבדקו ידנית מול dev server; דוח סיום בחלק ג'.

---

## חלק ג' - מה בוצע בפועל (2026-07-22)

### תיקון הזום (הבעיה שדווחה)
- הסיבה: כל השדות עם font-size של 14px ומטה; iOS Safari עושה auto-zoom לכל שדה מתחת ל-16px.
- התיקון (`globals.css`): במסכים עד 767px כל input/textarea/select מקבל 16px. בלי `user-scalable=no`, בלי `maximum-scale` - זום ידני של המשתמש נשאר פעיל. viewport מוגדר מפורשות ב-`layout.tsx` (כולל viewport-fit=cover ל-safe areas). חל על כל השדות בכל המסכים, כולל מסך הטוקן וה-playground הציבורי.

### מובייל
- ניווט תחתון (תיבת פניות / ידע / דשבורד / עוד) עם safe-area; מגירת "עוד" לשאר המסכים.
- שיחה פתוחה = מסך מלא (הכותרת והניווט מוסתרים), כפתור חזרה 44px עם aria-label.
- `visualViewport` hook מכווץ את הפריסה כשהמקלדת נפתחת - שדה ההקלדה תמיד גלוי (iOS וגם Chrome Android).
- שמירת מיקום גלילה ברשימה בחזרה משיחה; מסננים/חיפוש ב-sessionStorage (שורדים מעבר מסכים).

### תיבת פניות
- טיוטה פר-שיחה ב-localStorage (שורדת רענון; לא זולגת בין שיחות; נמחקת רק אחרי אישור שרת).
- מניעת שליחה כפולה (ref guard בצד לקוח + dedupe של 15 שניות בצד שרת ב-agentReply).
- כשל שליחה: הטיוטה נשארת + הודעת שגיאה + "נסה לשלוח שוב". ✓ על הודעות נציג = אושרו בשרת אחרי שליחה לערוץ.
- גלילה חכמה (autoscroll רק כשקרובים לתחתית; אחרת צ'יפ "הודעות חדשות ↓").
- הפרדת ימים (היום/אתמול/תאריך), dir=auto לתוכן מעורב, קישורים לחיצים בטוחים (https בלבד, noopener).
- unread dot לפי מעקב קריאה מקומי + "סמן כלא נקרא"; מסנן "סגורות" + "פתח מחדש"; ספירות במסנני ממתינות/הסלמות/נציג; skeletons בטעינה ראשונה; empty state אמיתי.
- כרטיס לקוח: נוספה עריכת שם, אישור לניקוי זיכרון, שגיאות שמירה גלויות.

### ידע (הפער המרכזי שנסגר)
- Backend חדש: `updateLearnedQA` (עריכת שאלה/תשובה) בשני ה-repos + עמודת `updated_at` + PATCH עם baseTs (409 על עריכה מקבילה) + POST ליצירת ידע יזומה.
- UI: עריכה מלאה עם שמירה/ביטול, עריכת ניסוח שאלה פתוחה, חיפוש, תאריכי יצירה/עדכון, "לא רלוונטית" עם אישור, "הוסף ידע", ואחרי כל שמירה - "בדוק עכשיו בבוט" שקופץ לבדיקת בוט עם השאלה מוכנה.
- אומת E2E: יצירה → עריכה → הבוט ענה עם התשובה המעודכנת (כולל המחיר החדש) בקריאת צ'אט אמיתית.

### ביצועים
- `getConversationSummaries()`: שאילתת SQL אחת (LATERAL + subqueries ממופתחות + אינדקס חדש `idx_messages_conv_ts`) במקום משיכת כל ההודעות כל 4 שניות. fallback אוטומטי לדרך הישנה אם השאילתה נכשלת. משמש את האינבוקס, בריאות הערוצים והסטטיסטיקות.

### אבטחה (כולל ממצאי סריקה ייעודית)
- **WhatsApp webhook: נוסף אימות חתימה** (לא היה בכלל!). אותו META_APP_SECRET כמו מסנג'ר/אינסטגרם.
- אימות חתימת Meta הפך fail-closed בפרודקשן (secret חסר = דחייה, לא דילוג). verify-token ריק נדחה.
- admin auth: fail-closed בפרודקשן בלי ADMIN_TOKEN, השוואת timingSafeEqual, יושם גם במסלול העלאת המדיה.
- `/api/chat` הציבורי: rate limit לפי IP (20/דקה), מגבלת אורך הודעה (2000), שגיאות פנימיות לא מוחזרות ללקוח.
- הגנת SSRF בתמלול קול (https בלבד, חסימת IP/localhost); escape ל-HTML באימייל ההסלמות.

### מסכים נוספים
- דשבורד: כרטיסי "ממתינות למענה" (מדד חדש אמיתי awaitingReplies) / "אצל נציג" / "שאלות ללמידה" לחיצים ומובילים לרשימה המסוננת.
- מידע עסקי: מעקב שינויים לא שמורים (אזהרה במעבר טאב + beforeunload), סרגל שמירה sticky שלא מכסה ניווט, "שמור" נדלק רק כשיש שינוי.
- מדיה: העתקת URL, אישור מחיקה, שדה URL ידני הוצנע ל"מתקדם", שגיאות שמירה גלויות.
- הגדרות: מתג בוט ראשי עם אישור, סטטוס ערוצים מפורט יותר, אזור מסוכן, שגיאות שמירת תבניות.
- בדיקת בוט: שגיאות שרת אמיתיות (לא "שגיאה בבדיקה" גנרית), session חדש = לקוח חדש (זיכרון לא זולג), prefill מהידע.

### בדיקות שהורצו
- `npm test` (חדש, tsx): 14/14 - repo summaries, CRUD ידע מלא, auth, חתימות, SSRF, רלוונטיות מדיה.
- E2E מול dev server אמיתי (file-repo עם 422 שיחות): settings, יצירת/עריכת ידע, 409 על דריסה, צ'אט אמיתי עם הידע המעודכן, רשימת שיחות, מענה נציג, idempotency, השהיה/סגירה/פתיחה מחדש, מחיקה.
- `npx tsc --noEmit` נקי; `npm run lint` נקי (ESLint הוגדר לראשונה: eslint 9 + next/core-web-vitals; unescaped-entities כבוי - טקסט עברי); `npm run build` נקי.

### הנחות ופתוחים לעתיד
- ההנחה: webhook ה-WhatsApp חתום באותו META_APP_SECRET (אותה אפליקציית Meta). לוודא אחרי deploy שהודעות WhatsApp נכנסות (אם לא - זו חתימה של אפליקציה אחרת).
- סטטוסי מסירה/קריאה מלאים (delivered/read) דורשים עיבוד אירועי statuses מה-webhooks - לא קיים היום בכוונה; המודל הנוכחי מציג רק מה שידוע באמת (נשלח-ואושר / נכשל).
- rate limit לפי IP הוא per-instance (serverless) - הגנת עלות בסיסית, לא מוחלטת.
- computeStats עדיין קורא את כל ההודעות (רק בדשבורד, כל 15 שניות) - מועמד לאופטימיזציה עתידית.
- מעקב "נקרא" הוא פר-מכשיר (localStorage) - מספיק לצוות קטן; מערכת משתמשים מלאה נשארת לעתיד.

### Rollback
- `git checkout <קומיט קודם>` או Vercel promote לדיפלוי הקודם. שינוי הסכימה היחיד: `ALTER TABLE learned_qa ADD COLUMN IF NOT EXISTS updated_at` + אינדקס - תוספות בלבד, לא שוברות את הגרסה הקודמת.
