# תוכנית שיפורים מקיפה - 27.8.2026

נכתבה בעקבות 6 ממצאים של אדיר. גובשה סביב סיבות-שורש, לא תיקונים נקודתיים.
נקודת שמירה קיימת: תג `backup-pre-batch-27-8` + `backups/2026-08-27/`.

## חלק א' - סיבות השורש (למה הבעיות האלה קיימות)

**RC1 - פעולות אמיתיות תלויות במודל.** פתיחת שער ויצירת הזמנה הן פעולות
שהעסק תלוי בהן, אבל השלמתן תלויה בכך שהמודל "יזכור" לקרוא לכלי ברגע הנכון.
כשהמודל מפספס (כרם - אסף הכל ואמר "בשמחה!" בלי לפתוח בקשה) אין רשת ביטחון.
העיקרון המתקן: **לכל פעולה קריטית חייב להיות מסלול דטרמיניסטי בקוד** -
המודל מנסח, הקוד מוודא שהפעולה קרתה.

**RC2 - זרימות מתות בשקט.** אין מנגנון שמזהה "הזרימה הזאת התחילה ולא
הסתיימה". הזמנה של כרם מתה בלי שאף אחד ידע. אותו סיכון קיים בכל זרימה
רב-שלבית. העיקרון: **צ'ק-פוינט קוד בסוף כל זרימה + ווצ'דוג שמסלים לצוות
כשזרימה נתקעה.**

**RC3 - וובהוקים מקבילים בלי אידמפוטנטיות ביצירה.** מטא שולחת כפולים;
הדה-דופ שלנו (mid בזיכרון + ב-DB) בודק *הודעות*, אבל *יצירת שיחה* לא
אידמפוטנטית - שתי הפעלות מקבילות ללקוח חדש יוצרות שתי שיחות (9 לקוחות
כפולים ב-3 ימים, כולם בחותמת זמן זהה). העיקרון: **כל יצירה חייבת מפתח
דטרמיניסטי או בדיקת קיום אטומית.**

**RC4 - טקסטים ללקוח גדלו בטלאים.** תשובת החניה צברה שכבות (תקציר + הוראות
מלאות + שער + סרטון + CTA) עד כפילות ואורך. אין בעלות אחת על "מה הלקוח
מקבל". העיקרון: **טקסט ללקוח נבנה פעם אחת, קצר, עם מקור אחד לכל רכיב.**

**RC5 - פעולות בלי תצפיתיות לצוות.** השער נפתח ונרשם רק כהודעת מערכת בשיחה
+ לוג בקונסול. אין מבט רוחבי. העיקרון: **כל פעולה פיזית נרשמת ביומן שהצוות
רואה.**

**RC6 - "עצירה בטוחה" שמייצרת תקלה חדשה.** הווטו על פתיחת שער מהמודל (תיקון
תקרית Liran) חסם את המקרה ההפוך - לקוח מתלונן שהשער לא נפתח ("???") והבוט
עונה "נראה שלא עבד" בלי לנסות שוב. תיקוני בטיחות חייבים לטפל גם בתרחיש
"הלקוח עדיין תקוע".

## חלק ב' - התיקונים

### 1. שני מספרי טלפון בכל הפניה להתקשרות
- helper אחד: `contactPhonesText(cfg, lang)` → "‎*8149 או 050-979-8917".
  מקור: cfg.contact.phone + cfg.contact.whatsapp (אותו קו = וואטסאפ העסק).
- מוחלף בכל טקסט ללקוח שמזכיר התקשרות: הודעות handoff, fallback של תקלה,
  rate-limit, חסימת/כשל שער, תבנית reserveHowto, וכלל 16 + פרטי הקשר בפרומפט
  ("כשנותנים מספר להתקשרות - תמיד את שניהם, לפעמים אין מענה באחד").
- לא נוגעים: מקרים שהם לא "תתקשרו" (קישורי וואטסאפ wa.me לבקשה מפורשת).

### 2. שער - "פתחת ולא נפתח"
- גלאי חדש לפני המודל: `isGateConfusionAfterOpen` - הודעת בלבול/תלונה קצרה
  (`???`, "לא נפתח", "לא עבד", "עדיין סגור", "not working"...) כאשר קיימת
  פתיחת שער ב-10 הדקות האחרונות בשיחה.
- פעולה: פתיחה חוזרת דטרמיניסטית (עוברת דרך אותה runGateOpen - מגבלת
  4/שעה נשמרת), עם נוסח "פתחתי שוב עכשיו 🙂 אם עדיין לא נפתח - תתקשרו
  ל-*8149 או 050-979-8917 ונעזור מיד".
- ניסיון חוזר שני נכשל/תלונה שלישית → הסלמה דחופה (המסלול הקיים של כשל שער).
- הווטו על נתיב המודל נשאר (מגן מפני פתיחות מדומיינות) - הבלבול מטופל עכשיו
  לפניו, בקוד.

### 3. הזמנות - הזרימה לא מתה בשקט (תקרית כרם)
שלוש שכבות:
- **פרומפט:** אחרי קבלת הפרט האחרון חובה לשלוח סיכום מלא + "הכל נכון?" -
  אסור לענות אישור קצר ("בשמחה!") בלי סיכום באמצע איסוף.
- **רשת ביטחון בקוד (א):** בזרימת הזמנה עם כל הפרטים מלאים (לפי
  extractReservationSlots) בלי הזמנה קיימת - אם המודל ענה תשובה קצרה בלי
  שאלה ובלי כלי, הקוד מחליף לסיכום דטרמיניסטי + "הכל נכון?".
- **רשת ביטחון בקוד (ב):** כל הפרטים מלאים + ההודעה הקודמת של הבוט הייתה
  סיכום עם בקשת אישור + הלקוח ענה אישור ("כן/נכון/מאשר/סבבה") + המודל שוב
  לא קרא לכלי → הקוד יוצר את ההזמנה בעצמו (createReservation) ושולח את
  נוסח הסיום התקני. דיוק הפרטים: מהחילוץ הקיים שכבר משמש כרמז למודל.
- **ווצ'דוג:** אם בכל זאת זוהתה זרימה עם פרטים מלאים שלא הבשילה להזמנה תוך
  4 הודעות לקוח נוספות → הסלמה שקטה לצוות ("ייתכן שהזמנה נפלה בין הכיסאות").
- תשובה לשאלת אדיר "בגלל שהוזלנו?": לא - ההודעות האלה הלכו למודל המלא.
  הסיבה: כלל-הברזל נגד הזמנות כפולות/מדומות דרש אישור לסיכום, והמודל שכח
  לשלוח את הסיכום. הרשתות למעלה סוגרות את החור בלי לוותר על ההגנה.

### 4. יומן פעילות שער בדשבורד
- רישום כל אירוע (פתיחה, חסימה, כשל) ל-settings `gate_log` (מערך, מוגבל
  ל-300 אחרונים): ts, customerName/id, ערוץ, תוצאה, dryRun.
- הרישום בתוך runGateOpen - נקודה אחת, תופסת את כל המסלולים.
- דשבורד: קטע "🅿️ פתיחות שער" - טבלת מי/מתי/תוצאה + מונה 7 ימים.
- אחורנית: אין (לא סורקים היסטוריה) - היומן מתחיל מהפריסה.

### 5. שיחות כפולות
- **מניעה:** מזהה שיחה דטרמיניסטי ללקוחות ערוצים אמיתיים ביצירה ראשונה:
  `c-<channel>-<channelUserId>`. שתי הפעלות מקבילות מתנגשות על אותו id -
  postgres: `ON CONFLICT (id) DO NOTHING` + שליפה חוזרת; file-repo: בדיקת
  קיום. (Playground ממשיך עם ה-id שהדפדפן שולח.)
- **ניקוי:** endpoint תחזוקה master-only שמאתר לקוחות עם כמה שיחות, מגבה את
  כולן ל-backups/, מעביר את כל ההודעות לוותיקה ביותר (repo חדש:
  reassignMessages) ומוחק את הריקות. מריצים פעם אחת, בודקים, משאירים כלי.
- 9 הלקוחות הקיימים ינוקו אחרי גיבוי ממוקד.

### 6. שכתוב תשובות החניה (לב עניין ה"שיח")
עקרונות: קצר וקולע; רכיב אחד לכל דבר (בלי כפילות שער); הסרטון עושה את
העבודה הקשה; תמיד הצעת פתיחת שער - פעם אחת.
- **תבניות חניה חדשות (3 גרסאות עברית, 2 אנגלית), מבנה אחיד:**
  [יש חניה חינמית צמודה] + [סרטון מצורף] + [שער סגור? תכתבו ואפתח].
  טיוטות (לביקורת הסוכנים):
  1. "יש לנו חניה חינמית גדולה צמודה למסעדה 🙂 הכניסה שלה קצת מוסתרת, אז
     מצרף סרטון קצר שמראה בדיוק איך נכנסים. ואם השער סגור כשתגיעו - תכתבו
     לי כאן ואפתח לכם 🙂"
  2. "בטח! יש חניה חינמית ממש ליד המסעדה. שולח סרטון קצר עם הדרך אליה 🙂
     ואם השער סגור - הודעה אחת כאן ואני פותח לכם 🙂"
  3. "יש חניה חינמית צמודה אלינו 🙂 היא קצת מתחבאת - הנה סרטון שמראה את
     הכניסה. שער סגור? תכתבו לי ואפתח לכם מיד 🙂"
- **בלי סרטון (fallback):** אותו מבנה עם קישור Waze במקום משפט הסרטון.
- **parking.summary בקונפיג החי מתקצר** לעובדות בלבד ("חניה חינמית גדולה
  צמודה, כניסה מעט מוסתרת") - הוראות ההגעה המפורטות נשארות ב-directions
  (למודל, כששואלים "איך בדיוק"). מסיר גם את שורת השער הישנה מה-summary -
  מקור אחד לשורת השער: gateOfferLine.
- **דה-דופ שער:** gateOfferLine לא מתווסף אם התשובה כבר מזכירה פתיחת שער.
- **תבניות המיקום** (הנוסחים של אדיר) נשארות - רק מוודאים שאין כפל שער.

## חלק ג' - איפה עוד הדפוסים האלה יכולים להופיע (נבדק כחלק מהביצוע)
- RC1/RC2: זרימת "שאלה לצוות" (ידע) - יש heuristic; זרימת שינוי הזמנה -
  מוסלמת תמיד (תקין). נבדק שאין זרימה רב-שלבית נוספת בלי צ'ק-פוינט.
- RC3: יצירת לקוח (upsert - אידמפוטנטי ✓), רישום הזמנה (id אקראי -
  נבדקת חשיפה לכפל וובהוק; ההגנה: דה-דופ ההודעה עצמה קודם), מנויי פוש
  (מפתח endpoint ✓).
- RC4: כל התבניות הקבועות ייסקרו לאורך/כפילויות בסקירת הסוכנים.
- RC5: פעולות "שליחת מדיה" ו"הסלמה" כבר נרשמות בשיחה; יומן רוחבי נוסף רק
  לשער (פעולה פיזית).

## חלק ד' - סדר ביצוע, אימות ופריסה
1. קוד ותשתית (5, 4): מניעת כפילויות + ניקוי; יומן שער + דשבורד.
2. לוגיקת שיחה (2, 3): שער-בלבול; רשתות ביטחון להזמנות.
3. טקסטים ופרומפט (1, 6): שני מספרים; תבניות חניה; עדכון קונפיג חי.
4. **פריסה אחת** לשינויי פרומפט/טקסט (חיסכון בכתיבת cache) + פריסות קוד
   לפי הצורך. כל פריסה מאומתת מול פרודקשן.
5. אימות: בדיקות מבודדות לכל רגקס/לוגיקה; E2E מקומי לזרימת הזמנה מלאה
   (כולל תרחיש כרם המשוחזר!) ותשובות חניה; **בלי E2E שער אמיתי** (בדיקות
   מבודדות + dry-run בלבד); הרצת חבילת eval מלאה לפני הפריסה הסופית +
   מקרים חדשים: כרם-flow, שני מספרים, חניה קצרה, שער-בלבול.
6. עדכון זיכרון + דוח סיום + רשימת "ממתין לאדיר".

## חלק ה' - סיכונים ובלמים
- יצירת הזמנה דטרמיניסטית: מופעלת רק אחרי אישור מפורש לסיכום שהוצג, עם כל
  הפרטים מלאים - אותו רף כמו המודל. בספק - לא יוצרים, מסלימים לצוות.
- פתיחה חוזרת של שער: רק בחלון 10 דקות מפתיחה קודמת + מגבלת 4/שעה קיימת.
- מיזוג שיחות: גיבוי מלא של המשתתפות לפני; ללא מחיקת הודעות לעולם.
- כל שינוי טקסט: eval + בדיקות ספוט לפני פריסה.
- rollback: תג הגיט + promote בורסל + השחזור המתועד ב-backups/.

---

## חלק ו' - הכרעות סופיות אחרי ביקורת 3 הסוכנים (27.8)

**מסוכן ההנדסה (השינוי המהותי):** כל מנגנוני המניעה היו "בדוק-ואז-פעל" -
נשבר בסביבה מרובת-שרתים. הפתרון השורשי - ייחודיות ברמת ה-DB:
1. אינדקס ייחודי חלקי על מזהה ההודעה של מטא (mid) - סוגר משלוחים כפולים
   בדלת הכניסה. נבנה ב-init עם try/catch (כפילויות קיימות לא מפילות את
   השרת; אחרי הניקוי האינדקס נתפס אוטומטית). ההכנסה עוברת ל-ON CONFLICT
   DO NOTHING; קונפליקט = שרת אחר מטפל בהודעה - עוצרים.
2. פתיחת שער חוזרת: claim אטומי - הודעת retry עם מפתח ייחודי על ההודעה
   המעוררת (meta.gateRetryOf); רק הזוכה מפעיל פיזית. **ריטריי אחד בלבד
   לכל פתיחה מקורית** (העוגן חייב להיות פתיחה שאינה retry) + העוגן בתוך
   3 ההודעות האחרונות + חותמת זמן אחרי הפתיחה + דילוג מלא על המודל.
   בלבול שני -> הסלמה, לא פתיחה.
3. הזמנה דטרמיניסטית: **נוצרת מתוך הסיכום שהבוט עצמו כתב** (פענוח הודעת
   הסיכום) עם הצלבה מול החילוץ - אי-הסכמה = לא יוצרים. חלון חילוץ מתאפס
   בסמן "הזמנה נוצרה"; תאריכים יחסיים מעוגנים לזמן ההודעה (Asia/Jerusalem
   כבר מטופל); בדיקת מדיניות ערב/שישי לפני; הדדופ הקיים פר-שיחה + אינדקס
   ה-mid סוגרים כפילויות.
4. יומן שער: **בלי מפתח settings בכלל** - שאילתה על הודעות עם
   meta.gateOpened/gateBlocked (מקור אמת אחד, שורד מיזוגים). חסימות וטו
   יקבלו meta שאילתתי.
5. מיזוג כפילויות: טרנזקציה אחת, עדכון updatedAt=הגדול, טאטוא הפניות
   (reservations.conversationId), דדופ הודעות זהות אחרי, לפני בניית
   אינדקס ה-mid. מזהה שיחה דטרמיניסטי עם hash (בלי טלפון גלוי ב-id).

**מסוכן השיח:** נוסחים סופיים נקבעו (4 גרסאות חניה עברית + Waze fallback +
2 אנגלית; "לחייג *8149 או 050-979-8917" בכל ההפניות; באנגלית הנייד קודם
כי כוכבית לא עובדת מסים זרים; סיכום הזמנה ברשימה מתויגת שעמידה בעברית;
"הכל נכון?" תמיד אחרון). מקסימום 🙂 אחד להודעה; שורת שער אחת לתשובה.

**מסוכן המוצר:** eval - עדכון המקרה הקיים שבודק רק 8149 + 4 מקרים חדשים +
תמיכת maxLen בראנר; הווצ'דוג מסלים דרך ערוץ ההתראות הקיים עם פרטי הלקוח;
לוג גם לחסימות (לא רק פתיחות); רשימת בדיקה-עצמית לאדיר בדוח הסיום.
נדחו במודע: אינדקס ייחודי על customer_id (סיכון init), רוטציית וריאנטים
חכמה (ההגרלה מספיקה), צמצום למספר וריאנטים (אדיר ביקש כמה סגנונות).

---

## סטטוס: בוצע במלואו ✅ (27.8)
כל ששת הסעיפים יושמו, נבדקו ופרוסים בפרודקשן. eval מלא: 83/83.
ניקוי כפילויות: 153 לקוחות (365 שיחות) מוזגו, גיבוי מלא ב-backups/.
פירוט מלא בדוח הסיום בשיחה ובזיכרון הפרויקט.
