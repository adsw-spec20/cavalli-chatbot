# תוכנית: להפוך את צ'אט המעבדה לכלי שאפשר לסמוך עליו

נכתב 24.9.2026, אחרי שהצוות הניהולי ראה את המעבדה ולא התרשם.

## מה באמת קרה בשיחה שנשברה

השאלה הייתה "כמה אי-הגעות וביטולים היו ב-23.9". אלה הכשלים, לפי סדר החומרה:

1. **הבוט הציג מספר של חלון מתגלגל כאילו הוא מספר של יום.** `tabit_no_show_summary`
   מקבל רק `days: N` ומחזיר צבירה שמסתיימת *עכשיו*. אין לו בכלל פרמטר יום.
   הלוג מאשר: הוא נקרא עם `{"days":1}`. התשובה "23.9: 14 אי-הגעות ו-46 ביטולים,
   סה״כ 296 הזמנות" היא צבירה של 24 שעות מתגלגלות, ו-296 כולל walk-ins והזמנות
   שהסתיימו. זו לא טעות ניסוח, זו המצאה.
2. **"אני לא יכול" על שאלה לגיטימית.** אין כלי שמחזיר אי-הגעות/ביטולים ליום
   מסוים, אז המודל המציא הסבר למה זה בלתי אפשרי ("23.9 היה לפני למעלה מחודש").
3. **ימי שבוע מסולפים.** הקוד חישב נכון, המודל שכתב את זה בפרוזה והזיז הכל ביום.
4. **הבטחות שהפרומפט נותן ואין להן כלי.** הפרומפט אומר שהבוט עונה על "הכנסות",
   אבל `revenue_summary`, `shift_dashboard` ו-`notification_status` קיימים בסוכן
   ולא נחשפים למודל בכלל.
5. **אפס שקיפות.** מספר מגיע בלי מקור, בלי טווח, בלי גיל נתונים. אי אפשר לאמת.
6. **איטיות.** כל שאלה עוברת דרך תור אל הסוכן המקומי שסורק עד כל 15 שניות,
   גם כשיש snapshot טרי במסד הנתונים.

## העיקרון

כל מה שמודל שפה גרוע בו - חשבון, סינון, ספירה, תאריכים, והחלטה מה הכלי כיסה -
נעשה בקוד. המודל רק בוחר כלי ומנסח. זה כבר העיקרון של `tabit-lab-smart.ts`,
הוא פשוט לא הגיע לשכבת הארכיון והאנליטיקה.

## שש השכבות

### 1. אמת: הנתון חייב להיות נכון
- כלי חדש `tabit_day_outcome`: אי-הגעות, ביטולים, הגעות ו-walk-ins **ליום
  קלנדרי מסוים**, מסונן לפי `reserved_from`, עם רשימות שמיות.
- חשיפת `revenue_summary`, `shift_dashboard`, `notification_status` למודל.
- `no_show_summary` הופך לכלי תקופה בלבד, ומצהיר על עצמו: מאיזה תאריך עד איזה,
  ו-`is_period_aggregate: true`. אם מועבר לו יום, השרת מנתב ל-`day_outcome`.
- **שער כיסוי:** הארכיון של טאביט מחזיר לפעמים חלון קצר מהמבוקש. היום זה נבלע
  בשקט ומחזיר אפס. מעכשיו חוסר כיסוי הוא שגיאה מפורשת, לא "אין".

### 2. שקיפות: חייב להיות אפשר לאמת
- כל תוצאת כלי עוטפת `_scope` (יום / טווח / תקופה / חי) ו-`_provenance`
  (כלי, מקור, גיל נתונים) שנבנים בקוד.
- כותרת מקור נבנית **בקוד** ונוספת לתשובה: על איזה יום, מאיזה מקור, ומתי נבדק.
- לוג הכלים בממשק הופך ל"איך זה נבדק" בעברית, במקום JSON גולמי.

### 3. מהירות
- Snapshot קודם: קריאות של היום/עתיד נענות מה-snapshot במסד (~200ms) במקום
  סבב אל הסוכן (5-20 שניות). נפילה חזרה לסוכן כשה-snapshot חסר או ישן מדי.
- הרצת כלים במקביל בתוך סבב.
- מטמון ארכיון ליום, כדי ששאלת המשך על אותו יום תהיה מיידית.

### 4. מודל ופרומפט
- מעבר ל-Sonnet 5.
- שכתוב הפרומפט: מ-21 כללים מסועפים למבנה הדוק. כל כלל שאפשר לאכוף בקוד, יורד
  מהפרומפט ונאכף בקוד.
- פענוח תאריכים דטרמיניסטי לכל הודעה, לא רק אחרי חצות.
- שאלת ההבהרה של חלון החצות נכתבת בקוד ולא על ידי המודל. שם בדיוק הוא סילף.

### 5. אימות
- סוללת הערכה מדורגת עם שכבת כלים מדומה: אופליין, חינם, דטרמיניסטית.
- בדיקות יחידה לכל לוגיקה טהורה חדשה.
- בדיקת עשן חיה מול פרודקשן.

### 6. חוויה
- אינדיקציה חיה מה הבוט עושה כרגע.
- צ'יפים שמשקפים שימוש אמיתי.

## מה שהתגלה תוך כדי, וחשוב יותר מהתוכנית

### הארכיון של טאביט כן נותן היסטוריה - הפרמטר נקרא until
מסקנה ראשונה שלי הייתה שגויה. כל בקשת ארכיון ארוכה מיממה חוזרת
`400 invalid requested time range (Nh > 24h)`, והסקתי שאין גישה להיסטוריה.
אדיר הצביע שבממשק רואים חודשיים אחורה. לכידת התעבורה של הממשק נתנה את התשובה:

```
/reservations-archived?from=2026-09-23T05:00:00.000Z&until=2026-09-23T21:14:00.000Z
```

המגבלה היא על **גודל החלון** (24 שעות), לא על כמה אחורה אפשר ללכת, והפרמטר
שסוגר את החלון נקרא `until`. שליחת `to` נבלעת בשקט, השרת מודד מ-from ועד
עכשיו, וכל יום ישן נדחה. חיפשתי את הגבול הלא נכון.

בפועל: יום בודד נקרא ב-0.9 שניות, שבוע ב-4 שניות, חודש ב-27 שניות (יום-יום).
הקוד הקודם, שניסה 30 ואז 14 ואז 7 ימים עד שמשהו החזיר 200, היה מצמצם בשקט את
החלון ומחזיר "אפס" על יום שמעולם לא נקרא.

### ההגדרות מכוילות למסך שהצוות מסתכל עליו
הממשק מראה ל-23.9 ארבעה ביטולים. בנתונים יש ארבע-עשרה רשומות עם סיבת ביטול,
אבל עשר מהן הזמנות אונליין **בלי שם ובלי טלפון** (ניסיונות שלא הושלמו) ואחת
היא מזדמן. ארבע הנותרות, של לקוחות אמיתיים, הן בדיוק מה שהממשק סופר.
אי-הגעות: המסנן של טאביט סופר גם מזדמנים, ולכן זה מה שהכלי מחזיר. הפירוק
להזמנות מול מזדמנים עבר לשדות נפרדים, לשאלות המשך.

### המספר האמיתי של 23.9
| | מה שהבוט אמר | מה שבטאביט |
|---|---|---|
| אי-הגעות | 14 | **14** (במסך: 15) |
| ביטולים | 46 | **4** |
| הזמנות ביום | 296 | **74** |

מה שהבוט אמר היה צבירה של חלון מתגלגל שכלל גם מזדמנים (190 מ-264 הרשומות של
אותו יום). הביטולים תואמים עכשיו בדיוק למסך. באי-הגעות נשאר פער של אחד
שעדיין לא הוסבר, וצריך להצליב מול רשימת השמות שבממשק.

### פנקס היומיים
נבנה כשחשבנו שאין היסטוריה: הסוכן צובר אצלנו את הרשומות בכל סבב, והשרת ממזג
(לא דורס - החלון מתגלגל). ההצלבה מול טאביט ליום אמיתי הראתה התאמה מלאה בכל
השדות. **הקריאה ממנו כבויה** (TABIT_LEDGER_READS), כי עכשיו שטאביט כן עונה,
שני מסלולי חישוב לאותה שאלה הם בדיוק הדרך שבה שני מספרים שונים מגיעים לאותו
צוות. האיסוף ממשיך כרשת ביטחון.

## מה נשאר בחוץ במכוון
פעולות כתיבה (יצירה, שינוי, ביטול) נשארות מושבתות. אי אפשר לבקש מהצוות לסמוך על
כתיבה לפני שהם סומכים על הקריאה.
